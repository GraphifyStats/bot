{
  "color": "#0094FF",
  "guildId": "YOUR SERVER ID",
  "environment": "dev",
  "channels": {
    "verify": "VERIFY CHANNEL ID",
    "doorway": "WELCOME AND LEAVE CHANNEL ID",
    "rules": "RULES CHANNEL ID (to be linked in the welcome message)",
    "roles": "ROLES CHANNEL ID"
  },
  "roles": {
    "member": "MEMBER ROLE ID"
  }
}
